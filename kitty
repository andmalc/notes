Themes to try:
Dimmed Monokai


Themes
	List
		https://github.com/dexpota/kitty-themes

	Test theme
		kitty @ set-colors -a themes/themename.conf

	Select by name
		kitten theme <name>

	Interactive chooser:
		kitten themes


Add kitty-terminfo to ssh login
	kitty +kitten ssh myserver


Kitty Shell
	Enter kitty terminal with "kitty @"
		get-colors
		set-colors foreground=x background=y
		exit - exit terminal and save
		goto-layout
		set-window-title
		set-font-size <num, 0 for default, +/- num>

	Command
		kitty @ --help		list of commands
		kitty @ <cmd>

Hints
	Ctrl+Shift+e - highlights URLs in text.  Click with mouse to open
